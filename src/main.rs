use pcm;

// nested paths
// use std::{cmp::Ordering, io};
// use std::io::{self, Write};

// glob operator : brings all public items from path into scope
// use std::collections::*;

fn main() {
    pcm::eat_at_restaurant();
    // pcm::hosting::add_to_waitlist();
    // pcm::serving::take_order();
}